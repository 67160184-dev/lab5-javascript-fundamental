# 📘 explain.md

เอกสารนี้อธิบาย **ผลลัพธ์ (Output)** และ **หลักการทำงานของโค้ด (How it works)** ในแต่ละไฟล์ ตามหัวข้อที่กำหนด เพื่อให้สามารถเปิดอ่านและตรวจสอบได้โดยตรงบน GitHub

---

## 2.1 `01-variables.js`

### 6. Challenge: Create a Person Object

### 🔎 โค้ดทำอะไร

สร้าง object `student` ที่เก็บข้อมูลนักเรียน และมี **method** สำหรับคำนวณ/แสดงผลข้อมูล

```js
const student = {
  firstName: "Alice",
  lastName: "Smith",
  age: 20,
  gpa: 3.8,
  courses: ["HTML", "CSS", "JavaScript"],
  isActive: true,
  getFullName: function () {
    return `${this.firstName} ${this.lastName}`;
  },
  getInfo: function () {
    return `${this.getFullName()}, Age: ${this.age}, GPA: ${this.gpa}`;
  },
};
```

### 📤 ผลลัพธ์ที่แสดง

- แสดง object `student`
- แสดงชื่อเต็มจาก `getFullName()`
- แสดงข้อมูลรวมจาก `getInfo()`
- แสดงรายวิชาด้วย `join()`

### 🧠 อธิบายการทำงาน

- ใช้ **object literal** ในการเก็บข้อมูลหลายชนิด
- `this` อ้างอิง object ปัจจุบัน
- method เรียก method อื่นภายใน object ได้

---

## 2.2 `02-functions.js`

### 8. Returning Objects

#### 🔎 โค้ดทำอะไร

ฟังก์ชัน `createUser` คืนค่าเป็น object ผู้ใช้ พร้อม method

```js
function createUser(firstName, lastName, age) {
  return {
    firstName,
    lastName,
    age,
    email: `${firstName.toLowerCase()}.${lastName.toLowerCase()}@example.com`,
    getFullName() {
      return `${this.firstName} ${this.lastName}`;
    },
  };
}
```

#### 📤 ผลลัพธ์

- ได้ object ผู้ใช้ใหม่
- แสดง email และชื่อเต็ม

#### 🧠 อธิบายการทำงาน

- ใช้ **property shorthand**
- คืนค่า object เพื่อใช้งานต่อ
- method ใช้ `this` เข้าถึงข้อมูล

---

### 9. Function as Parameter (Callback)

#### 🔎 โค้ดทำอะไร

ส่งฟังก์ชันเข้าไปเป็นพารามิเตอร์

```js
function processArray(arr, callback) {
  const result = [];
  for (const item of arr) {
    result.push(callback(item));
  }
  return result;
}
```

#### 📤 ผลลัพธ์

- Array ที่ถูกแปลงค่า (เช่น คูณ 2, ยกกำลังสอง)

#### 🧠 อธิบายการทำงาน

- callback ถูกเรียกทีละสมาชิก
- เป็นแนวคิด **Higher-order function**

---

## 2.3 `03-control-flow.js`

### 5. Short-Circuit Evaluation

#### 🔎 โค้ดทำอะไร

ใช้ `||`, `&&`, `?.` เพื่อป้องกัน error

```js
const userName = admin?.name || user.name || "Anonymous";
```

#### 📤 ผลลัพธ์

- แสดงชื่อ "John"

#### 🧠 อธิบายการทำงาน

- `?.` ป้องกันการเข้าถึง object ที่เป็น `null`
- `||` เลือกค่าที่เป็น truthy ตัวแรก

---

### 7. Form Validation

#### 🔎 โค้ดทำอะไร

ตรวจสอบข้อมูลฟอร์ม และเก็บ error ไว้ใน array

#### 📤 ผลลัพธ์

- user ถูกต้อง → `isValid: true`
- user ผิด → แสดง error หลายรายการ

#### 🧠 อธิบายการทำงาน

- ใช้ if/else หลายเงื่อนไข
- คืนค่า object สรุปผลการตรวจสอบ

---

## 2.4 `04-loops.js`

### 9. Chaining methods

#### 🔎 โค้ดทำอะไร

เชื่อม `filter → map → join`

```js
const evenStrings = data
  .filter((n) => n % 2 === 0)
  .map((n) => `${n}²=${n * n}`)
  .join(", ");
```

#### 📤 ผลลัพธ์

```
2²=4, 4²=16, 6²=36, 8²=64, 10²=100
```

#### 🧠 อธิบายการทำงาน

- แต่ละ method คืน array ใหม่
- สามารถ chain ต่อได้

---

### 10. Challenge: Student Grades

#### 🔎 โค้ดทำอะไร

วิเคราะห์คะแนนนักเรียน

- map → ดึงชื่อ
- filter → คนคะแนนสูง
- reduce → ค่าเฉลี่ย / คนคะแนนสูงสุด
- sort → เรียงคะแนน

#### 📤 ผลลัพธ์

- ค่าเฉลี่ยห้อง
- คนคะแนนสูงสุด
- ตารางสรุปพร้อมเกรด

---

## 2.5 `05-integration.js`

### Activity 5: Quiz Application

### 🔎 โค้ดทำอะไร

- จำลองระบบ Quiz
- สุ่มคำตอบผู้ใช้
- ตรวจคำตอบ
- คำนวณคะแนน เกรด และสถิติ

### 📤 ผลลัพธ์

- แสดงผลแต่ละข้อ (ถูก/ผิด)
- คะแนนรวม (%)
- เกรด (A–F)
- สถิติ Correct / Incorrect

### 🧠 อธิบายการทำงาน

- ใช้ `forEach` ประมวลผลคำถาม
- ใช้ `filter` และ `reduce` คำนวณสถิติ
- ใช้ if/else กำหนดเกรด

---

## ✅ สรุป

ไฟล์ `explain.md` นี้แสดงให้เห็นว่า **โค้ดแต่ละส่วนทำงานอย่างไร และแสดงผลลัพธ์ออกมาได้อย่างไร** โดยสามารถเปิดอ่านได้โดยตรงบน GitHub เพื่อใช้ประกอบการเรียนและการตรวจงาน

🎉 **End of Explanation**
